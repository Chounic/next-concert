import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import { ReactElement } from 'react'
import ConcertImage from '../components/ConcertImage'
import Layout from '../components/Layout'
import styles from '../styles/Home.module.css'


const Home = ({data}: any) => {
  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex-row flex-wrap grid grid-cols-4 gap-3">
{  data.results.map( (item: any, index: any) => {
  console.log("🚀 ~ file: index.tsx ~ line 21 ~ Home ~ item", item)
  return (
    <div key={index} style={{ backgroundColor: 'green', border: '5px dashed black'}}>
      <ConcertImage data={item}/>
      <p>{item.title}</p>
      <p>{item.category}</p>
      <p>{item.category}</p>
    </div>
  )
})
}

      </main>
    </div>
  )
}

Home.getLayout = function getLayout(page: ReactElement) {
  return (
    <Layout>
      {page}
    </Layout>
  )
}

export async function getServerSideProps() {

  console.log("🚀 ~ file: index.tsx ~ line 35 ~ getServerSideProps ~ test")
  const res = await fetch(`https://api.predicthq.com/v1/events?limit=30&category=concerts&location_around.origin=48.856614,-2.3522219&location_around.offset=50km`, {
    headers: {
      Authorization: `Bearer ${process.env.ACCESS_TOKEN}`, 
      Accept: 'application/json', 
      'Content-Type': 'application/json'
    }
  }); 
  console.log("🚀 ~ file: index.tsx ~ line 40 ~ getServerSideProps ~ process.env.ACCESS_TOKEN", process.env.ACCESS_TOKEN)
  console.log("🚀 ~ file: index.tsx ~ line 44 ~ getServerSideProps ~ res", res)
  const data = await res.json();
  return { props: { data } }
  
}

export default Home
